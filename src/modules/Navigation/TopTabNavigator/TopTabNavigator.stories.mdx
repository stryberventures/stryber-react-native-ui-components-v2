import {Meta} from '@storybook/addon-docs';
import pkg from './package.json';

<Meta title="Modules/Navigation/TopTabNavigator/README" parameters={{
  previewTabs: {
    canvas: {hidden: true},
  },
  viewMode: 'docs',
  pkg,
}} />

# React Navigation

We are using [react-navigation](https://reactnavigation.org/) package for our `Top Tab Navigator`

## Material Top Tabs Navigator

Please check [Material Top Tabs Navigator](https://reactnavigation.org/docs/material-top-tab-navigator) docs to install it to your project.

And then copy code of [this module](?path=/docs/modules-navigation-toptabnavigator--top-tab) to your project
