import {Meta} from '@storybook/addon-docs';
import pkg from './package.json';

<Meta title="Modules/Navigation/SideNavigator/README" parameters={{
  previewTabs: {
    canvas: {hidden: true},
  },
  viewMode: 'docs',
  pkg,
}} />

# React Navigation

We are using [react-navigation](https://reactnavigation.org/) package for our `Drawer Navigator`

## Drawer Navigator

Please check [Drawer Navigator](https://reactnavigation.org/docs/drawer-navigator) docs to install it to your project.

And then copy code of [this module](?path=/docs/modules-navigation-drawernavigator--drawer) to your project
