import {Meta} from '@storybook/addon-docs';
import pkg from './package.json';

<Meta title="Modules/Navigation/BottomTabNavigator/README" parameters={{
  previewTabs: {
    canvas: {hidden: true},
  },
  viewMode: 'docs',
  pkg,
}} />

# React Navigation

We are using [react-navigation](https://reactnavigation.org/) package for our `Bottom Tabs Navigator`

## Bottom Tabs Navigator

Please check [Bottom Tabs Navigator](https://reactnavigation.org/docs/bottom-tab-navigator) docs to install it to your project.

And then copy code of [this module](?path=/docs/modules-navigation-bottomtabnavigator--bottom-tab) to your project
