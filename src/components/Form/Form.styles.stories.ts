import {createUseStyles} from '../Theme';

export default createUseStyles({
  scrollViewContainer: {
    paddingHorizontal: 8,
  },
  buttonWrapper: {
    marginTop: 20,
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
});
