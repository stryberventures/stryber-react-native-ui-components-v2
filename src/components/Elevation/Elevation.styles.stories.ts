import {createUseStyles} from '../Theme';

export default createUseStyles({
  card: {
    height: 216,
    width: 216,
    backgroundColor: '#fff',
  },
  centeredContainer: {
    alignItems: 'center',
  },
  borderRadius: {
    borderRadius: 16,
  },
});
