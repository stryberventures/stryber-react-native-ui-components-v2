import {createUseStyles} from '../Theme';

export default createUseStyles({
  validationContainer: {
    marginTop: 8,
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  chip: {
    marginRight: 10,
    marginTop: 8,
  },
});
