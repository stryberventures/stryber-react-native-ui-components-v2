import {createUseStyles} from '../../../components/Theme';

export default createUseStyles({
  container: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  value: {
    fontSize: 24,
    marginHorizontal: 10,
  },
  title: {
    marginTop: 20,
    marginBottom: 10,
    fontSize: 20,
    textAlign: 'center',
  },
});
